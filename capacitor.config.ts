import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Takout-mercado',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
